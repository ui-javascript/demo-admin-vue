<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="space.qmen.leaf.dao.UserRoleDao">
    <resultMap id="BaseResultMap" type="space.qmen.leaf.domain.UserRole">
        <result column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="role_id" property="roleId" />
        <result column="status" property="status" />
    </resultMap>

    <parameterMap id="UserRole" type="space.qmen.leaf.domain.UserRole"/>

    <sql id="Base_Column_List">
        id, user_id, role_id, status
    </sql>

    <select id="findById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="Base_Column_List" />
        from user_role
        where id = #{id}
    </select>

    <select id="findAllUserRole" resultMap="BaseResultMap" >
        select
        <include refid="Base_Column_List" />
        from user_role
    </select>

    <insert id="saveUserRole" parameterMap="UserRole" useGeneratedKeys="true" keyProperty="id">
        insert into
        user_role(id, user_id, role_id, status)
        values
        (#{id},#{userId},#{roleId},#{status})
    </insert>

    <update id="updateUserRole" parameterMap="UserRole">
        update
        user_role
        set
        <if test="userId!=null">
            user_id = #{userId},
        </if>
        <if test="roleId!=null">
            role_id = #{roleId},
        </if>
        <if test="status!=null">
            status = #{status}
        </if>
        where
        id = #{id}
    </update>

    <delete id="deleteUserRole" parameterType="java.lang.Long">
        delete from
        user_role
        where
        id = #{id}
    </delete>
</mapper>