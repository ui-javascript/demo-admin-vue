## 数据源配置
spring:
  main:
    banner-mode: "off"
#  profiles: dev
  datasource:
    url: jdbc:mysql://47.100.99.127:3306/lot_share?useUnicode=true&characterEncoding=utf8&autoReconnect=true&useSSL=false
    username: luojs
    password: ljc578762
    driver-class-name: com.mysql.jdbc.Driver

## Mybatis 配置
mybatis:
  typeAliasesPackage: space.qmen.lot.entity
  basePackage: space.qmen.lot.mapper
  mapperLocations: classpath*:space/qmen/lot/mapper/*Mapper.xml
  configuration:
    map-underscore-to-camel-case: true #开启驼峰映射

mapper:
    mappers:
        - tk.mybatis.springboot.util.MyMapper
    not-empty: false
    identity: MYSQL


# 分页配置
pagehelper:
  helper-dialect: mysql
  reasonable: true
  support-methods-arguments: true
  params: count=countSql



# 打印sql
logging:
  level:
    com:
      space.qmen.lot.dao: debug