<%
const $ = require('../src/mock/$')(view)
const $h = require('../src/mock/$h')
%>

<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta name="keywords" content="<%= $.head.keywords %>">
  <meta name="description" content="<%= $.head.description %>">
  <title><%= $.head.title %></title>
  <%- $h.getStyles('common') %>
  <%- $h.getScripts('common') %>
  <%- $h.getStyles($.styles) %>
  <%- $h.getScripts($.scripts) %>
</head>
<body>
